; Test letrec with factorial function
; (letrec ((fact (lambda (n) (if (= n 0) 1 (* n (fact (- n 1))))))) (fact 5))
; Should return 120
(letrec ((fact (lambda (n)
  (if (= n 0)
    1
    (* n (fact (- n 1)))))))
  (fact 5))
